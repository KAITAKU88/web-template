
Hiện tại, khi mở trang landing page của sản phẩm, thì ảnh đầu tiên lại hiển thị không đúng tỷ lệ 16:9 của ảnh.
Xong thì commit , không push 
Hiện tại tôi phát hiện ra nhiều nút bấm khi được tương tác lại không hiển thị trực quan, hãy kiểm tra toàn bộ các nút bấm trong hệ thống, cần phải trực quan khi hover hoặc tối thiểu là khi nút được click chọn thì phải hiển thị khác đi để báo hiệu cho người dùng biết rằng nút đó hoặc link đó đã được click. 
Xong thì commit, không push 
Tiếp tục xem phần sidebar của dashboard, khi nôi dung nhiều hơn thì nút Đăng xuất bị đẩy xuống phía dưới, hãy sửa lại để cho phép sidebar cũng có thể cuộn scroll được, việc cuộn sidebar cần độc lập so với phần nội dung bên phải.
Đồng thời hiện tại Dashboard đang mặc định dark theme, hãy cho phép chỉnh sửa theme giống như trên trang web.
Xong thì tiến hành commit, không push 
Sau khi làm xong việc này thì tiếp tục rà soát lại toàn bộ hệ thống 1 lần nữa, theo huồng hoạt động, cần chỉnh sửa điều gì thì hãy tự động thực hiện.
Xong việc nếu không bị lỗi gì thì tiến hành commit và push, ngày mai tôi sẽ thực hiện các thiết lập thủ công còn lại để hệ thống chạy hoàn hảo.